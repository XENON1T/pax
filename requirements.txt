wheel>=0.23.0
numpyson             # Needed for micromodels
numpy>=1.9.0         # Needed for np.unique(a, return_counts=True) in waveform simulator
pymongo
tables>=3.1.1
blosc
matplotlib>=1.3.0
scipy>=0.14.0
tqdm                 # Progress bar library
prettytable
pandas               # Used for the waveformsimulator's truth file
